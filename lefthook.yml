# lefthook.yml

pre-commit:
  commands:
    check:
      run: npm run format {staged_files}
      glob: '*.{js,jsx,mjs,ts,tsx,css,json}'
      stage_fixed: true

pre-push:
  commands:
    validate:
      glob: '*.{ts,tsx}'
      run: npm run lint
